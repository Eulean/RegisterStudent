@model StudentRegisteration.ViewModels.CourseViewModel

<h1>Create Course</h1>

@if (ViewData.ModelState.Any())
{
    <div class="alert alert-danger">
        <ul>
            @foreach(var error in ViewData.ModelState.Values.SelectMany(v => v.Errors))
            {
                <li>@error</li>
            }
        </ul>
    </div>
}

<form method="post">
    <div class="form-group">
        <label for="Course">Course</label>
        <input type="text" class="form-control" id="Course" name="Course" required>
    </div>
    <div class="form-group">
        <label for="StudentYear">StudentYear</label>
        <input type="text" class="form-control" id="StudentYear" name="StudentYear" required>
    </div>
    <div class="form-group">
        <label for="Semester">Semester</label>
        <input type="text" class="form-control" id="Semester" name="Semester" required>
    </div>
    <div class="form-group">
        <label for="Cost">Cost</label>
        <input type="text" class="form-control" id="Cost" name="Cost" required>
    </div>

    <button type="submit" class="btn btn-primary">Create</button>

</form>

@* @Su Yin 
make those input dropdonw with option for all 
eg cost gonna have 3 selectable option 10000 20000 30000 etc  *@
 